<Window x:Class="PresentationLayer.Views.DeleteCustomerPopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationLayer"
        mc:Ignorable="d"
        Title="Deleting Customer" Height="200" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock Text="Are you sure you want to delete this Customer ?" FontSize="20" HorizontalAlignment="Center"/>
        </Grid>
    
        <Grid Grid.Row="1">
            <Button Content="Yes" Click="deleteButton_Click" Margin="10,54,386,37"/>
            <Button Content="No" Click="closeButton_Click" Margin="386,54,10,37" />
        </Grid>

    </Grid>
</Window>
